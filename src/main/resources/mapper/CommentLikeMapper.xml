<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coding.mapper.CommentLikeMapper">

    <select id="selectByCommentIdAndUserId" resultType="com.coding.entity.CommentLike">
        SELECT id, comment_id, user_id, create_time
        FROM comment_like
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </select>

    <delete id="deleteByCommentIdAndUserId">
        DELETE FROM comment_like WHERE comment_id = #{commentId} AND user_id = #{userId}
    </delete>
</mapper>
