<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coding.mapper.TagMapper">

    <select id="selectHotTags" resultType="com.coding.entity.Tag">
        SELECT tag_id, name, color, article_count, create_time, update_time, deleted
        FROM tag
        WHERE deleted = 0
        ORDER BY article_count DESC
        LIMIT 10
    </select>

    <!-- 更新标签的文章数（仅统计已发布且未删除的文章） -->
    <update id="updateArticleCount">
        UPDATE `tag` t
        SET `article_count` = (
            SELECT COUNT(DISTINCT at.article_id)
            FROM `article_tag` at
            INNER JOIN `article` a ON at.article_id = a.article_id
            WHERE at.tag_id = #{tagId}
            AND a.status = 1
            AND a.deleted = 0
        )
        WHERE t.tag_id = #{tagId}
    </update>

</mapper>

